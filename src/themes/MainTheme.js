import { createTheme } from "@mui/material";

const MainTheme = createTheme({
  palette: {
    primary: {
      main: "#1C8AA3",
    },
    secondary: {
      main: "#373737",
    },
  },
});

export default MainTheme;
